# 📂 ملخص مشروع نظام إدارة عروض الأسعار (Modern Design System)

## 🏗️ الهيكل العام ومواقع الملفات (File Structure)

 اسم الملف  المسار (Path)  الوظيفة الأساسية 
 ---  ---  --- 
 Login  `pagesQuotationModernDesignlogin.js`  صفحة الدخول تتحقق من اسم المستخدم وكلمة المرور وصلاحية الحساب (نشطموقف). 
 Dashboard  `pagesQuotationModernDesigndashboard.js`  لوحة التحكم تعرض الفواتير (للمدير يرى الكل، وللمصمم يرى فواتيره فقط). 
 CreateEdit  `pagesQuotationModernDesigncreate.js`  صفحة الإنشاء نموذج إضافة بنود العرض، الحسابات التلقائية، وحفظ العرض في السيرفر. 
 User Mgmt  `pagesQuotationModernDesignusers.js`  إدارة الطاقم صفحة خاصة للمدير لإضافة مصممين، تعيين كلمات مرور، وإيقاف حساباتهم. 
 User API  `apiusersmanage.js`  المحرك الخلفي للمستخدمين يعالج عمليات (إنشاء، حذف، تحديث، قراءة) المستخدمين من ملف JSON. 
 Quotes API  `apiquotesadmin.js`  المحرك الخلفي للعروض يعالج حفظ وجلب بيانات عروض الأسعار. 
 Storage  `data_storagesettingsusers.json`  قاعدة بيانات المستخدمين تخزن (الاسم، كلمة المرور، الصلاحية Role، الحالة Status). 
 Quotes Data  `data_storagequotes.json`  قاعدة بيانات العروض تخزن تفاصيل كل عرض سعر (العميل، البنود، التاريخ، المُنشئ). 

---

## 🛠️ ما تم إنجازه (System Capabilities)

### 1. نظام الحماية والصلاحيات (RBAC)
- المدير (Admin) يملك صلاحية كاملة لرؤية كل عروض أسعار الموظفين، وإدارة حسابات المصممين (إضافةحذفتعطيل).
- المصمم (Designer) يملك صلاحية إنشاء عروض أسعار خاصة به، ولا تظهر له في لوحة التحكم إلا العروض التي أنشأها هو فقط.

### 2. إدارة المستخدمين
- تم ربط تسجيل الدخول بكلمة مرور (Password) واسم مستخدم.
- إضافة ميزة الحالة (Status) يمكن للمدير تعطيل حساب أي موظف فوراً ليمنعه من دخول النظام دون حذفه.

### 3. ذكاء البيانات (Data Logic)
- ربط كل فاتورة بـ مُنشئ (createdBy) النظام يسجل تلقائياً من هو الموظف الذي قام بعمل هذا العرض لضمان المتابعة.
- معالجة تلقائية لحساب المدير في حال فقدان ملف الإعدادات، النظام يقوم تلقائياً بإنشاء حساب admin بصلاحيات كاملة.

### 4. واجهة الاستخدام (UI)
- اعتماد اللون البراند (#18907A).
- تصميم جداول احترافية تدعم اللغة العربية (RTL) مع أزرار واضحة للإجراءات.